#include "simple_shell.h"
/**
*
*
**/
char* _strtok(char *buffer)
{/*
    int i, j;
    char *c;

    for (i = 0; buffer[i] != '\0'; i++)
    for (j = 0;; j++)
    {
        if (buffer[i] == "")
        {

        }
        else
        {
            c[j] = buffer[i];
        }
    }

    return ()*/
}
